#include<stdio.h>
/**
* main - print size of various types
* Return: the 0 (Success)
*/

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; ++digit)
	{
		printf("%d", digit);
	}
	printf("\n");
	return (0);
}
